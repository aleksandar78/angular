import { AttributeHandlerDirective } from './attribute-handler.directive';

describe('AttributeHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
